// Primeiro arquivo em linguagem C
// Vamos importar a biblioteca base para executar o nosso programa
// A biblioteca se chama stdio(std->Standard | i -> input | o -> output)
// Para importar, usaremos os comandos #include
#include <stdio.h>

// Abaixo iremos escrever os comandos que serão realmente executados
// Ultilizaremos a função main(principal) para iniciar a execução
int main(){
    // O comando printf exibir uma mensagem em tela de console
    printf("Hello,World! E o pix, nada ainda? \n"); 

    // Informa o compilador (Tradutor) gcc que o programa acabou
    return 0;

}

